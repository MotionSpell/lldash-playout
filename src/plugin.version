FOO {
  # explicitly list symbols to be exported
  global:

    lldplay_create;
    lldplay_destroy;
    lldplay_play;

    lldplay_get_stream_count;
    lldplay_get_stream_info;

    lldplay_enable_stream;
    lldplay_disable_stream;

    lldplay_grab_frame;

    lldplay_get_version;

  # hide everything else
  local: *;
};
