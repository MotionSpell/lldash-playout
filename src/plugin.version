FOO {
  # explicitly list symbols to be exported
  global:

    sub_create;
    sub_destroy;
    sub_play;

    sub_get_stream_count;
    sub_get_stream_info;

    sub_enable_stream;
    sub_disable_stream;

    sub_grab_frame;

    sub_get_version;

  # hide everything else
  local: *;
};
